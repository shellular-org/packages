{
  "name": "packages",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev:cli": "pnpm --filter protocol run build && cd cli && pnpm run dev",
    "build": "pnpm -r run build",
    "ci:publish": "pnpm -r run ci:publish",
    "format": "pnpx @biomejs/biome check --write"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@biomejs/biome": "^2.4.13",
    "@changesets/cli": "^2.31.0"
  }
}
